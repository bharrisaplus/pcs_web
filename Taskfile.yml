# Be sure to have direnv allow the .envrc file before running this
#   OR load the variables from .envrc yourself

# yaml-language-server: $schema=https://taskfile.dev/schema.json

# Running commands looks like:
# task TARGET:OBJECTIVE:action-AREA 
# 
# (see build/README.md)
# 

version: '3'

includes:
  octocat: ./Build/octocat/_tasks.yml

tasks:
  check_env:
    cmds:
      - echo $PROJECT_DIR
      - echo $BEHAVIOR_DIR
      - echo "Unix timestamp {{now | unixEpoch}}"

  # Top level aliases for convenience

  rear-octocat-latest-fhost:
    aliases: [rear-file]
    cmds:
      - task: octocat:latest:rear-fhost

  rear-octocat-latest-lhost:
    aliases: [rear-local]
    cmds:
      - task: octocat:latest:rear-lhost

  rear-octocat-latest-prod:
    aliases: [rear-prod]
    cmds:
      - task: octocat:latest:rear-prod

  sweep-octocat-latest-fhost:
    aliases: [sweep-file]
    cmds:
      - task: octocat:latest:sweep-fhost

  sweep-octocat-latest-lhost:
    aliases: [sweep-local]
    cmds:
      - task: octocat:latest:sweep-lhost

  sweep-octocat-latest-prod:
    aliases: [sweep-prod]
    cmds:
      - task: octocat:latest:sweep-prod